I want a UX then for my app, so please generate me the UX that will be used by me to build the webiste using the AI, so please generate me the UX.
 PROMPT :
Build a fully dynamic, production-ready school website for MONTESSORI HIGH SCHOOL (SSC), Tallapudi, Kukunuru, Andhra Pradesh 534341, using React (Vite) for the frontend and Firebase (Authentication, Firestore, Storage, Cloud Functions, App Check) for the backend, ensuring that the entire public website is data-driven from Firestore and that every change made by the Admin is reflected automatically and immediately on the public site without redeployment; implement a private Admin panel accessible only at /admin, which must first display a secure email/password login page using Firebase Authentication, and only authenticated users with a custom claim role: "admin" may access the dashboard, while all others are redirected to the homepage; for development purposes seed a single admin user with email karteekdunga@gmail.com and password test123 (implemented safely via emulator or bootstrap script, not hardcoded insecurely in production), and enforce that all database writes (create, edit, publish, delete) can only be performed by authenticated admin users through Firestore rules; design the system so that every record created from the Admin UI (announcements, gallery images, student life posts, faculty profiles, syllabus entries, academic calendar entries, rankers, highest scorer banner, results metadata) is stored properly in Firestore collections and/or Storage with structured metadata including status (draft/published/expired), createdAt, updatedAt, and any contextual fields (year, subject, etc.), and ensure that the public website queries only published documents using real-time listeners (onSnapshot) so that whenever the Admin clicks “Publish,” the Firestore document updates and the public site UI updates instantly without refresh; the public website must behave like a professional educational institution website similar in experience to Raghu Engineering College (structured header, top info bar, sticky navigation, institutional hero section, clean typography, card-based sections, academic color theme), and its components must dynamically render Firestore content, meaning that the Hero section, Announcements section, Gallery slideshow, Student Life cards, Faculty listing, Syllabus page (structured content), Academic Calendar page (year-wise selection), Rankers section, and Highest Scorer banner all automatically reflect admin actions; define a clear Draft vs Published logic where public pages filter only status == "published" and hide expired items automatically (especially announcements), and when Admin deletes content it must be removed from Firestore so it disappears immediately from the public UI; for Results, ensure student-level data is stored privately in a restricted Firestore collection and accessed only through a secure HTTPS Cloud Function endpoint (no public direct reads), while the Results page itself becomes visible only after the Admin publishes a batch in resultsMeta; the Admin dashboard must contain modules corresponding to each public section and provide Create, Edit, Save Draft, Publish, and Delete workflows, with visual status badges and real-time feedback, and all actions must persist to Firestore and trigger automatic UI updates; the public site must be entirely reactive to database state, structured with reusable React components (Header, Navbar, Hero, SectionWrapper, CardGrid, AnnouncementList, GallerySlider, FacultyGrid, SyllabusViewer, CalendarViewer, RankersDisplay, Footer), and must behave as a live content portal where the Admin panel is the single source of truth and Firestore acts as the central data layer that synchronizes admin actions to the public interface in real time, creating a robust, scalable, educational-grade CMS-driven website with no manual redeploy required for content updates.